# LM Studio – **host** endpoint, NOT a container name
LMSTUDIO_HOST=host.docker.internal   # macOS special DNS name
LMSTUDIO_PORT=12345                  # whatever port LM Studio is serving on

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Agent API
AGENT_MODEL=gpt-4o-mini            # just a label; the real model runs in LM Studio
AGENT_OPENAI_API_KEY=dummy          # any non‑empty string works
AGENT_MAX_LOOKUP_HISTORY=500

# UI
UI_PORT=3000                         # host port for the React UI
