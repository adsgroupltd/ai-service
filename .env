# -------------------------------------------------
# LM Studio – host endpoint (LM Studio runs outside Docker)
# -------------------------------------------------
LMSTUDIO_HOST=host.docker.internal   # macOS special DNS name for the host
LMSTUDIO_PORT=12345                  # <-- change if your LM Studio uses another port

# -------------------------------------------------
# Redis
# -------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379

# -------------------------------------------------
# Agent API (FastAPI wrapper)
# -------------------------------------------------
AGENT_MODEL=gpt-4o-mini            # just a label; real model runs in LM Studio
AGENT_OPENAI_API_KEY=dummy          # any non‑empty string works for LM Studio
AGENT_MAX_LOOKUP_HISTORY=500

# -------------------------------------------------
# UI
# -------------------------------------------------
UI_PORT=3000                        # host port where the React UI will be served

